# Your Discord bot token (Found at https://discord.com/developers/applications)
# 您的 Discord 機器人令牌（在 https://discord.com/developers/applications 找到）
# 您的 Discord 机器人令牌（在 https://discord.com/developers/applications 找到）
botToken: ""

# Your Discord bot client ID
# 您的 Discord 機器人客戶端 ID
# 您的 Discord 机器人客户端 ID
clientId: ""

# Your Genius API client access token (Found at https://genius.com/developers)
# 您的 Genius API 客戶端訪問令牌（在 https://genius.com/developers 找到）
# 您的 Genius API 客户端访问令牌（在 https://genius.com/developers 找到）
geniusApiKey: ""

# The colour to use for embeds sent by the bot
# 機器人發送的嵌入式消息使用的顏色
# 机器人发送的嵌入式消息使用的颜色
embedColour: "#F44336"

# Whether the bot should autocomplete search results
# Disable this if you're experiencing Unknown Interaction errors
# 機器人是否應該自動完成搜索結果
# 如果遇到未知的交互錯誤，請禁用此功能
# 机器人是否应该自动完成搜索结果
# 如果遇到未知的交互错误，请禁用此功能
enableAutocomplete: true

# Settings to control the bot's player and controller
# 控制機器人播放器和控制器的設置
# 控制机器人播放器和控制器的设置
player:
    # Leave VC when a song ends
    # 當歌曲結束時離開語音頻道
    # 当歌曲结束时离开语音频道
    leaveOnEndDelay: 300000 # 5 minutes
    # Leave VC when a song is stopped
    # 當歌曲停止時離開語音頻道
    # 当歌曲停止时离开语音频道
    leaveOnStopDelay: 300000 # 5 minutes
    # Leave VC when the channel is empty
    # 當頻道為空時離開語音頻道
    # 当频道为空时离开语音频道
    leaveOnEmptyDelay: 300000 # 5 minutes
    # Deafen bot while playing
    # 播放時對機器人拒聽
    # 播放时对机器人拒听
    deafenBot: true

# The emojis to be used in buttons for certain embeds
# 用於某些嵌入式消息中按鈕的表情符號
# 用于某些嵌入式消息中按钮的表情符号
emojis:
    stop: "⏹"
    skip: "⏭"
    queue: "📜"
    pause: "⏯"
    lyrics: "📜"
    back: "⏮"
    autoplay: "»"
    shuffle: "🔀"

# Make Guizhong use a proxy when making requests.
# Useful if you're experiencing 429 errors.
# Omit user:pass@ if your proxy does not require authentication.
# 使 Guizhong 在進行請求時使用代理。
# 如果您遇到 429 錯誤，這很有用。
# 如果您的代理不需要身份驗證，請省略 user:pass@
# 使 Guizhong 在进行请求时使用代理。
# 如果您遇到 429 错误，这很有用。
# 如果您的代理不需要身份验证，请省略 user:pass@
proxy:
    enable: false
    connectionUrl: "http://user:pass@111.111.111.111:8080"

# Make Guizhong use a custom YouTube cookie when making requests.
# This can help to prevent against receiving 429 rate limits from YouTube.
# 使 Guizhong 在進行請求時使用自定義 YouTube cookie。
# 這可以幫助防止收到 YouTube 的 429 速率限制。
# 使 Guizhong 在进行请求时使用自定义 YouTube cookie。
# 这可以帮助防止收到 YouTube 的 429 速率限制。
cookies:
    useCustomCookie: false
    youtubeCookie: ""

# Your YouTube API key (Found at https://console.developers.google.com/)
# 您的 YouTube API 金鑰（在 https://console.developers.google.com/ 找到）
# 您的 YouTube API 密钥（在 https://console.developers.google.com/ 找到）
youtubeapikey: ""

# Your Google API key (Found at https://console.developers.google.com/)
# 您的 Google API 金鑰（在 https://console.developers.google.com/ 找到）
# 您的 Google API 密钥（在 https://console.developers.google.com/ 找到）
googleapikey: ""

# Your OpenAI API key (Found at https://platform.openai.com/)
# 您的 OpenAI API 金鑰（在 https://platform.openai.com/ 找到）
# 您的 OpenAI API 密钥（在 https://platform.openai.com/ 找到）
openaiapikey: ""

# YouTube OAuth tokens
# YouTube OAuth 令牌
# YouTube OAuth 令牌
YT_ACCESS_TOKEN: ""
YT_REFRESH_TOKEN: ""
oauthTokens: ""

# LLM Provider API Keys
# LLM 提供商 API 密钥
# LLM 提供商 API 密鑰

# GitHub Model API Token
# GitHub Model API 令牌
# GitHub Model API 令牌
githubToken: ""

# Google Gemini API Key (single key)
# Google Gemini API 密钥（單個）
# Google Gemini API 密鑰（單個）
geminiApiKey: ""

# Google Gemini API Keys (multiple keys for rotation/load balancing)
# Google Gemini API 密钥（多個，用於輪流/負載均衡）
# Google Gemini API 密鑰（多個，用於輪流/負載均衡）
# 方式一：使用陣列
# geminiApiKeys:
#   - "key1"
#   - "key2"
#   - "key3"
# 方式二：使用逗號分隔的字串（在 geminiApiKey 中）
# geminiApiKey: "key1,key2,key3"
geminiApiKeys: []

# Ollama API Endpoint (default: http://localhost:11434)
# Ollama API 端点（默认: http://localhost:11434）
# Ollama API 端點（預設: http://localhost:11434）
ollamaEndpoint: "http://localhost:11434"

# Groq API Key
# Groq API 密钥
# Groq API 密鑰
groqApiKey: ""

# OpenRouter API Key
# OpenRouter API 密钥
# OpenRouter API 密鑰
openRouterApiKey: ""

# Zhipu AI API Key (Get your API key at https://bigmodel.cn/usercenter/proj-mgmt/apikeys)
# 智谱 AI API 密钥（在 https://bigmodel.cn/usercenter/proj-mgmt/apikeys 获取 API 密钥）
# Zhipu AI API 密鑰（在 https://bigmodel.cn/usercenter/proj-mgmt/apikeys 獲取 API 密鑰）
zhipuApiKey: ""

# Cloudflare API Key
# Cloudflare API 密钥
# Cloudflare API 密鑰
cloudflareApiKey: ""

# Cloudflare API Endpoint (default: https://api.cloudflare.com/client/v4/accounts/yourid/ai/run/@cf/black-forest-labs/flux-1-schnell)
# Cloudflare API 端点（默认: https://api.cloudflare.com/client/v4/accounts/yourid/ai/run/@cf/black-forest-labs/flux-1-schnell）
# Cloudflare API 端點（預設: https://api.cloudflare.com/client/v4/accounts/yourid/ai/run/@cf/black-forest-labs/flux-1-schnell）
cloudflareEndpoint: ""

# Tavily API Key (Get your free API key at https://app.tavily.com/)
# Tavily API 密钥（在 https://app.tavily.com/ 获取免费 API 密钥）
# Tavily API 密鑰（在 https://app.tavily.com/ 獲取免費 API 密鑰）
tavilyApiKey: ""


# Database configurations
# 數據庫配置
# 数据库配置

# MongoDB URI for database connection
# MongoDB URI 用於數據庫連接
# MongoDB URI 用于数据库连接
mongoURI: ""

# Base URL for the Web Viewer (Next.js)
# 用於 Web 檢視（Next.js）的基底網址
# 用于 Web 视图（Next.js）的基底网址
webBaseUrl: ""

# Logging settings
# 日誌設置
# 日志设置
logging:
  # Whether to show YouTubeJS error messages in the logs
  # 是否在日誌中顯示 YouTubeJS 錯誤訊息
  # 是否在日志中显示 YouTubeJS 错误信息
    showYoutubeJSErrors: false